# ── Database ─────────────────────────────────────────────────────────
MONGO_URI=mongodb+srv://adnaninsky:980Orwo7M5xB8fnh@cluster0.bd9ywas.mongodb.net/krishi-lms

# ── Security (REQUIRED — must be changed before deploy) ──────────────
# Generate a strong secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_64_CHARACTER_STRING

# ── Admin Account ─────────────────────────────────────────────────────
ADMIN_EMAIL=admin@krishividya.com
ADMIN_PASSWORD=Admin@123

# ── CORS (REQUIRED in production — set to your frontend domain) ───────
# Example: ALLOWED_ORIGINS=https://krishividya.com
ALLOWED_ORIGINS=http://localhost:5173

# ── Frontend URL (used for bKash callback redirect) ───────────────────
FRONTEND_URL=http://localhost:5173

# ── Debug (MUST be false in production) ──────────────────────────────
FLASK_DEBUG=false
FLASK_ENV=production

# ── Rate limiting (optional Redis backend for multi-worker) ──────────
# RATELIMIT_STORAGE_URI=redis://localhost:6379/0

# ── Cloudinary (REQUIRED for image/video/file storage) ───────────────
# Sign up free at https://cloudinary.com — find these in your dashboard
CLOUDINARY_CLOUD_NAME=dy8kariwa
CLOUDINARY_API_KEY=279216853178921
CLOUDINARY_API_SECRET=lDGBl1SHNqwjET_AEb4pAb4Kz90

# ── bKash Merchant API (Tokenized Checkout) ───────────────────────────
# Sandbox: https://tokenized.sandbox.bka.sh/v1.2.0-beta
# Production: https://tokenized.pay.bka.sh/v1.2.0-beta
BKASH_BASE_URL=https://tokenized.sandbox.bka.sh/v1.2.0-beta
BKASH_APP_KEY=your_bkash_app_key
BKASH_APP_SECRET=your_bkash_app_secret
BKASH_USERNAME=your_bkash_username
BKASH_PASSWORD=your_bkash_password
# NOTE: If bKash credentials are not set, the system uses DEMO MODE
